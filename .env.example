DEEPSEEK_API_KEY=sk-your-key

# ADX account tokens (used when connecting to a real API)
ADX_RS_TOKEN=
ADX_AP_TOKEN=

# Email settings (used when MOCK_EMAIL=false)
MOCK_EMAIL=true
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=robot@example.com
SMTP_PASSWORD=
SMTP_FROM=robot@example.com
